# Server-only. Never expose these to the browser.
PADDLE_API_KEY=
# Used only when you click Fetch or Re-fetch exchange rates.
CURRENCYAPI_API_KEY=
# Required. Must be "sandbox" or "production". The SDK defaults to production if omitted.
PADDLE_ENVIRONMENT=sandbox
